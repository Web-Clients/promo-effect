/**
 * Application-wide constants — replaces magic numbers scattered across components.
 *
 * Task C8 — Phase C Code Quality
 */

// Pagination
export const DEFAULT_PAGE_SIZE = 10;
export const BULK_FETCH_LIMIT = 100;

// Dashboard
export const RECENT_BOOKINGS_COUNT = 5;

// Invoices
export const DEFAULT_INVOICE_DUE_DAYS = 30;

// Pricing / Calculator
export const OFFER_AVAILABILITY_THRESHOLD = 0.95;

// UI
export const TOAST_DURATION_MS = 4000;
export const SEARCH_DEBOUNCE_MS = 300;
